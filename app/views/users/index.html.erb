<!--logged_in?が反応してない？なんで-->
<% if logged_in? %>
  <p> <%= current_user.username %>がログイン中</p>
  <%= render 'layouts/toppage' %>
<% else %>
  <p>ログインしていません。</p>
  <%= render 'layouts/toppage' %>
<% end %>

